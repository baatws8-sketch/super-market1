import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.supermarket.engineer',
  appName: 'com.supermarket.engineer',
  webDir: 'dist'
};

export default config;
